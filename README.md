# openapi-python-client-docker

Package triaxtec/openapi-python-client into a docker image

## Usage

```bash
docker pull mirakurunchan/openapi-python-client
docker run -u ${UID}:${GROUPS} -v ${PWD}:/out openapi-python-client-gen
```
